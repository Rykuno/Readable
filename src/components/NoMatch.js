import React from 'react';
import PageNotFound from '../images/page-not-found.jpg';

const NoMatch = () => (
  <div>
    <img className="center" src={PageNotFound} alt="Page Not Found" />
  </div>
);

export default NoMatch;
